import { FormEvent, useState } from 'react'
import { adminLogin } from '@/lib/api'
export default function AdminLogin({ onNavigate }: { onNavigate: (path: string) => void }) { const [identifier, setIdentifier] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); async function submit(e: FormEvent) { e.preventDefault(); setError(''); setLoading(true); try { const result = await adminLogin(identifier, password); localStorage.setItem('sql_admin_token', result.token); onNavigate('/admin') } catch (err) { setError(err instanceof Error ? err.message : 'Não foi possível entrar') } finally { setLoading(false) } } return <main className="admin-auth"><form className="auth-card" onSubmit={submit}><a className="back-link" href="/">← Software Quality Lab</a><div className="eyebrow">PRIVATE AREA</div><h1>Admin</h1><p>Gerencie artigos e case studies do projeto.</p><label>Email<input type="email" value={identifier} onChange={(e) => setIdentifier(e.target.value)} required autoComplete="username" /></label><label>Senha<input type="password" value={password} onChange={(e) => setPassword(e.target.value)} required autoComplete="current-password" /></label>{error && <p className="form-error" role="alert">{error}</p>}<button className="button button-primary" disabled={loading}>{loading ? 'Entrando…' : 'Entrar'}</button></form></main> }
