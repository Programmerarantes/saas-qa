import type { Locale } from '@/lib/i18n'
import { BRAND } from '@/lib/branding'
export default function Contact({ locale }: { locale: Locale }) { return <section className="section-wrap prose-page"><div className="eyebrow">CONTACT</div><h1>{locale === 'pt-BR' ? 'Vamos manter contato.' : 'Let’s stay in touch.'}</h1><p className="page-lead">{locale === 'pt-BR' ? 'Formulário de contato não faz parte da V1. Use os canais configuráveis abaixo.' : 'A contact form is intentionally out of scope for V1. Use the configurable channels below.'}</p><div className="contact-links"><a href={BRAND.github} target="_blank" rel="noreferrer">GitHub ↗</a><a href={BRAND.linkedin} target="_blank" rel="noreferrer">LinkedIn ↗</a><a href={`mailto:${BRAND.email}`}>{BRAND.email}</a><a href={BRAND.resume}>Download CV ↓</a></div></section> }
