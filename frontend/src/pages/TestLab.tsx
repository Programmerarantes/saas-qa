import { translations, type Locale } from '@/lib/i18n'
export default function TestLab({ locale, onNavigate }: { locale: Locale; onNavigate: (path: string) => void }) { const t = translations[locale]; return <section className="section-wrap lab-page"><div className="eyebrow">EXPERIMENTS</div><h1>{t.nav.lab}</h1><p className="page-lead">{t.labIntro}</p><article className="lab-card"><div className="lab-index">LAB / 01</div><div><h2>Authentication Lab</h2><p>{locale === 'pt-BR' ? 'Uma pequena aplicação de autenticação para investigar sessões, lockout, logout, expiração e requests autenticadas.' : 'A small authentication application for investigating sessions, lockout, logout, expiration and authenticated requests.'}</p><a className="button button-primary" href="/test-lab/authentication" onClick={(e) => { e.preventDefault(); onNavigate('/test-lab/authentication') }}>{locale === 'pt-BR' ? 'Abrir aplicação' : 'Open application'} →</a></div></article><p className="lab-footnote">{locale === 'pt-BR' ? 'Os Labs são implementados em código e evoluem com testes, commits, CI e deploy.' : 'Labs are implemented in code and evolve through tests, commits, CI and deployment.'}</p></section> }
